# Accreta

`AGENTS.md` temporal — se está reescribiendo. Lo único que fija por ahora:

**`muckpile` no sigue nada de este archivo.** Reemplaza a `worklist`, tiene su propio ADR (`subsystems/muckpile/.stratum/impl/docs/adr/0001-muckpile-nace-como-subsistema-propio.md`) y su propio `AGENTS.md`, que viaja con su repo (`git@github.com:anibalanto/muckpile.git`). Trabajando ahí, mandan sus reglas, no las de acá.
