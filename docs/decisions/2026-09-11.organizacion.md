# organizacion — Una carpeta por organización, con sus proyectos adentro

**Estado:** Borrador · **Versión:** 0.1 · **Última actualización:** 2026-09-11

**Dónde cae.** Acá, en la raíz de accreta, porque cruza a `muckpile` y al método. No tiene otras partes todavía.

---

## Contexto

**accreta es, además de specs y herramientas, el orquestador de una organización.** Quien trabaja para una empresa arma una carpeta para ella, por ejemplo `~/Workspace/lamansys/`, con la identidad que la empresa le da y los proyectos en los que trabaja: `sge`, `hsi`. Cada proyecto es un board.

**`muckpile` ya tiene esa carpeta, pero sin nombre ni identidad propios.** El ADR-0001 de `muckpile` la llama `multitask/`: la carpeta que junta los proyectos. Y `identity.toml` repite el email en cada proyecto, aunque el email lo da la empresa y no cada board.

**Y hay algo que la organización necesita y ningún proyecto puede dar:** una vista de lo que puede haber para mí en todos los proyectos a la vez. Hoy toda vista de `muckpile` es de un solo proyecto, incluso las consultas.

**Cómo carga Claude Code lo que necesita una sesión.** Medido en su documentación el 2026-09-11:

| Qué | Desde la carpeta donde arranca la sesión, hasta dónde sube |
|---|---|
| `CLAUDE.md` | Hasta la raíz del filesystem. |
| `.claude/skills/` | Solo hasta la raíz del repositorio. |

Una vista de `muckpile` es un worktree de `.muckpile/`, así que su raíz de repositorio es la vista misma. Un `CLAUDE.md` en la organización llega a todas las vistas, y un `.claude/skills` en la organización no llega a ninguna.

---

## Decisión

### 1. Tres niveles: organización, proyecto y vista

```
~/Workspace/lamansys/            ← la organización: la arma accreta init
  CLAUDE.md  AGENTS.md           ← llegan a toda sesión que arranque adentro
  projects/                      ← un proyecto por board
    sge/                         ← lo arma muckpile init sge
      .muckpile/  muckpile.toml
      base/  backlog/  to-work/  query/
    hsi/
  <instrumento>/                 ← a futuro, al lado de projects/
```

**La raíz de la organización tiene solo nombres fijos:** `projects/`, y a futuro cada instrumento con el suyo. Todo nombre que varía, como el de un proyecto, está un nivel adentro de `projects/`. Es la misma regla que ya sigue la raíz de un proyecto de `muckpile`, y por la misma razón: un proyecto nunca choca con un instrumento.

**`muckpile init <proyecto>` corre en `projects/`,** que es la carpeta que junta los proyectos. Nada más de `muckpile` cambia.

**En este texto, "accreta" es la herramienta y "la organización" es la carpeta que arma.**

**Avance: 0/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| La raíz de la organización tiene solo `projects/` y nombres fijos | `pendiente` | — | — |

### 2. La identidad es de la organización

**El email lo da la organización, y todos sus proyectos lo heredan.** `accreta init --email <email> --token-env <VARIABLE>` la registra una vez. Un proyecto que necesita otra cuenta la declara él.

**El token nunca es un argumento ni está en un archivo.** Como argumento quedaría en el historial del shell, se vería en `ps` y pasaría por el contexto de un agente. `init` guarda el nombre de la variable, y el valor se lee del entorno en el momento, como hoy en `identity.toml`. Si al correr `init` la variable no está en el entorno, lo dice.

**Avance: 0/2.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `accreta init` registra el email y el nombre de la variable del token, y los proyectos los heredan | `pendiente` | — | — |
| El token no es un argumento de ningún comando | `pendiente` | — | — |

### 3. Dos inits: `accreta init` en la organización y `muckpile init` en cada proyecto

- **`accreta init`, en la carpeta de la organización:** registra la identidad, crea `projects/`, y genera `AGENTS.md` desde la plantilla de accreta y un `CLAUDE.md` que lo incluye con `@AGENTS.md`. Como `CLAUDE.md` se carga desde las carpetas padre, llega a toda sesión que arranque adentro.
- **`muckpile init <proyecto>`, en `projects/`:** crea el registro, `muckpile.toml` y las carpetas reservadas, como hoy.

Esto afina la decisión 16 de `decisiones-vivas`, que pone `AGENTS.md` y `CLAUDE.md` en la carpeta donde arranca cada sesión: con la organización, van una sola vez, arriba de todo.

**Avance: 0/2.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `accreta init` registra la identidad, crea `projects/` y genera `AGENTS.md` y `CLAUDE.md` | `pendiente` | — | — |
| `muckpile init` corre en `projects/` y hereda la identidad | `pendiente` | — | — |

### 4. Las skills se enlazan en cada vista — abierta

**Qué está en juego.** Claude Code busca `.claude/skills` solo hasta la raíz del repositorio, y cada vista es su propio worktree. Las skills tienen que estar en la vista.

**Opciones que se conocen:**
- `to-work`, `sprint fetch` y `pull query/…` enlazan `.claude/skills` al abrir una vista, y lo excluyen en `.muckpile/`. `muckpile` sabe entonces de Claude Code.
- `muckpile` corre `accreta init` sobre la vista al abrirla, y es `accreta` quien sabe de Claude Code.
- La persona corre `accreta init` en la vista antes de arrancar la sesión.

**Avance: 0/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Quién enlaza las skills en cada vista | `abierta` | — | — |

### 5. Dónde vive la identidad de la organización — abierta

**Qué está en juego.** Hoy la identidad está en `~/.config/muckpile/identity.toml`, fuera de toda carpeta de trabajo, y tiene una tabla por proyecto.

**Opciones que se conocen:**
- En la carpeta de la organización, fuera de todo repo, por ejemplo `lamansys/.accreta/identity.toml`.
- En `~/.config/accreta/`, con una tabla por organización.

**Avance: 0/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Dónde vive la identidad de la organización | `abierta` | — | — |

### 6. La hipervisión: lo que puede haber para mí — abierta

**Qué está en juego.** Una vista de la organización que cruza proyectos: por ejemplo, las `question` que me bloquean, lo asignado a mí y lo que me menciona, en `sge` y en `hsi` a la vez. Sería el primer instrumento al lado de `projects/`. Todavía no está definida, y hoy lo único que hay son `question` como tareas en Jira.

**Avance: 0/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Qué muestra la hipervisión, y dónde vive | `abierta` | — | — |

### 7. `accreta-cli`: dónde vive el código y dónde se publica — abierta

**Qué está en juego.** `accreta init` es el primer comando de un CLI propio de accreta, que va a sumar más.

**Opciones que se conocen:**
- Como impl de la raíz, en `.stratum/impl/`, con `.stratum/.impl.toml`.
- Como subsistema, en `subsystems/accreta-cli/`.

Queda además quién crea el repo `anibalanto/accreta-cli` en GitHub, y si `subsystems/muckpile/.stratum/.muckpile.toml` pasa a llamarse `.impl.toml`, para que coincida con el directorio `impl`.

**Avance: 0/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Dónde vive el código de `accreta-cli`, y en qué repo | `abierta` | — | — |

---

## Lo que esta decisión no decide

- **Cómo llega una sesión a las skills y a `AGENTS.md` fuera de una organización,** por ejemplo en la raíz de accreta. Es de la decisión 16 de `decisiones-vivas`.

---

## Historial

| Versión | Fecha | Qué cambió |
|---|---|---|
| 0.1 | 2026-09-11 | Primer borrador: la organización como carpeta con `projects/` adentro, la identidad a nivel organización, los dos inits, y cómo carga Claude Code `CLAUDE.md` y las skills, medido en su documentación. Abiertas: quién enlaza las skills en cada vista, dónde vive la identidad, la hipervisión y `accreta-cli`. |
