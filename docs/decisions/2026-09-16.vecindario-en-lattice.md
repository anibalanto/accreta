# vecindario-en-lattice — Lo aceptado del vecindario es de bilinker, y lo derivado es de lattice

**Estado:** En desarrollo · **Versión:** 1.1 · **Última actualización:** 2026-09-16

**Dónde cae.** Acá, en la raíz de accreta, porque cruza bilinker, lattice y lspd. La parte de bilinker está en `*/subsystems/bilinker>impl`, con las secciones 1, 2, 3, 4, 6, 7 y 9. La de `lspd` es `levantar-el-daemon` § 5, en `*/subsystems/lspd>impl`. La de lattice está en `*/subsystems/lattice>impl`, con las secciones 7 y 8.

---

## Contexto

**El vecindario de nivel 1 —qué tipos del proyecto menciona la firma de un fragmento— es de bilinker de punta a punta**, y eso choca con lo que bilinker dice de sí mismo: opera sólo con git y tree-sitter, y el grafo del proyecto es de lattice.

**Qué hace bilinker con el nivel 1.** Medido el 2026-09-16 sobre `bdf3fbc`:

| Parte | Qué hace | Dónde |
|---|---|---|
| Adquirir: dónde preguntar | Sube del fragmento a la firma que lo contiene y baja a los identificadores de tipo del retorno y los parámetros. Contesta `None`, `At` o `Unreachable` sin proveedor. | `bilinker/src/neighbours.rs` (`reach`, `type_positions`), `grammar.rs` |
| Adquirir: qué declara cada tipo | El puerto `Neighbours`, y su único adaptador, que le pide `definitions` a `lspd`, levanta el daemon una vez por corrida y espera sólo el que levantó. | `bilinker/src/neighbours.rs` (el trait), `bilinker-cli/src/lspd_neighbours.rs`, `main.rs` |
| Guardar | `n` declarado y `accepted[].n`: `declined` o los niveles, cada uno con `link`, `hash` y `hash_ast`. El fold captura cada vecino y pliega por id de capture. `accept` acuña los captures y decide qué `n` escribe. | `bilink-format/src/bilink.rs`, `schema.rs`; `bilinker/src/neighbours.rs` (`fold`); `accept.rs` (`resolve_n1`) |
| Verificar | El eje del vecindario, sólo con el contenido `OK`: ubicación por ids, sin proveedor, y contenido recalculando el fold, con proveedor. De ahí salen los cinco `CONTRACT_*`. | `bilinker/src/check.rs` (`compare_contract`), `state.rs` |
| Repuntar | `apply` le pregunta al proveedor el conjunto de hoy y repunta `n.1.link`, y también sube la cobertura donde no había nivel. | `bilinker/src/apply.rs` (`neighbourhood_fix`) |
| Acarrear | `adopt` trae la declaración `vecindario` y la decisión; `pull` une el YAML sin mirarlo; `restore-n1` devuelve lo que tiró la `003`; `capture prune` no borra un capture que sólo nombra un vecindario; `get` lo muestra resolviendo los captures, sin proveedor. | `bilinker/src/adopt.rs`, `bilink_ref.rs`, `restore_n1.rs`, `capture.rs`, `get.rs`; `bilink-migrate/src/cut.rs`, `partition.rs`, `accepted_list.rs` |

**Hoy `check` levanta el daemon.** Le pasa el proveedor al puerto, y `compare_contract` le pregunta por cada nivel 1 aceptado cuyo fragmento dio `OK`, aunque el conjunto sea vacío.

**Cuánto se usa.** Medido el 2026-09-16:

| Capa | Endpoints aceptados | Sin `n` | `declined` | Nivel 1 vacío | Con vecinos |
|---|---|---|---|---|---|
| `bilinker` (`bdf3fbc`), `lattice` (`93b4582`) y `accreta` (`81acc9b`) | 610 | 429 | 133 | 27 | 21 |
| `sge` (`d5a748c7fe`, bilinks front ↔ back) | 110 | 2 | 108 | 0 | 0 |

En `sge` los 108 `declined` tienen firma alcanzable —`reach` da `At` en todos— y salieron de `accept --no-n1 .` en bloque, sin ningún daemon en esa copia. Si se hubiera adquirido, 52 back y 41 front habrían tenido al menos un vecino de la capa, y dos tercios de las 371 posiciones caen fuera de ella (`Observable`, `ResponseEntity`, `Short`, `Integer`). Los números de accreta subestiman el uso: en un proyecto con DTOs, el nivel 1 aplica a casi todo.

**Qué contesta lattice.** Medido el 2026-09-16 sobre `93b4582`, con `rust-analyzer` listo: `lattice graph crates/lattice/src/provider.rs:241:8 --depth 1` devuelve una sola arista, `call` hacia `call_edges`, con el nodo recortado al byte donde arranca la línea, y ninguna hacia `NodeId` ni `Edge`. Sus `kind` son `bilink`, `governs`, `issue`, `call`, `doclink` y `external`. El daemon sí lo sabe: `definitions` en esas posiciones devuelve la línea 54 de `model.rs` para `NodeId` y la 114 para `Edge`, y fuera de la capa para `Path`, `Result` y `Vec`.

**Lattice no distingue "no pude" de "no hay" en una pregunta.** `Registry::expand` descarta con `.ok()` el error de `edges_from`, `call_edges` deserializa con `unwrap_or_default()`, y `anchor_of` sin posición contesta vacío. `LspProvider::available` dice `Available` ante cualquier daemon que conteste el ping.

**Y hoy no llega a un bilink por los llamadores.** `edges_from` se pide sólo sobre los nodos de partida, así que `call` tiene un salto. Un rango de varias partes, como el de un capture `spring-controller`, sale de bilinker como `a~b,c~d`, y `NodeId::as_fragment` no lo parsea: `covers` y `contains` dan falso.

**Lo que lattice lee de bilinker está roto.** Medido el 2026-09-16: `bilinker graph . --format json` contesta `no bilinks found for '.'` con 194 bilinks, y sale con 0. `find_graph_starts` filtra la extensión `bilink`, y los bilinks son `.yaml`. Lattice recibe un stdout vacío y el proveedor `bilink` queda `Unavailable` con `EOF while parsing a value`.

**Los captures de los vecinos prueban menos que el daemon.** Resolverlos con tree-sitter dice que las declaraciones aceptadas no cambiaron, pero no que los nombres de la firma sigan resolviendo a ellas: eso depende de los imports del archivo, de lo que declara el mismo paquete y de las dependencias del build, que están fuera del fragmento. En `sge`, `AsistenciaResponse` está declarado dos veces en Java.

---

## Decisión

### 1. El corte es por garantía: lo aceptado es de bilinker, lo derivado es de lattice

**Bilinker se queda con los niveles que alguien aceptó y se verifican contra un baseline: el 0 y el 1.** Adquiere, guarda, verifica, repunta y acarrea el nivel 1, y conserva el adaptador de `lspd` detrás del puerto `Neighbours`.

**Lattice se queda con lo que se deriva sin aceptar:** llamadores, quién menciona un tipo, y cualquier profundidad mayor. Contesta *"puede estar afectado"*; el *"está roto"* lo pone el estado de bilinker, alcanzado por contención.

**Se descarta un nivel mayor que 1 aceptado.** Un fold sobre los llamadores a varios saltos se mueve con cualquier cambio del sistema, y un aviso que sale siempre no lo lee nadie. Además va en el sentido contrario: el nivel 1 baja de la firma a sus tipos, y el impacto sube del cambio a quien lo llama.

**Se descarta, por ahora, que lattice sea el proveedor del puerto.** Los dos ya le hablan a `lspd` por `lspd-client`, así que pasar por lattice no le saca a bilinker la dependencia: la vuelve indirecta, y agrega una dependencia de crates de bilinker hacia lattice que hoy no existe. Queda posible cuando `mentions` (§ 8) dé sus garantías: el puerto existe para cambiar de proveedor.

**Se descarta que bilinker deje el eje y lo consulte en lattice.** Lattice no persiste nada y sus aristas `derived` no tienen estado aceptado: llevarle el eje es volver a inventar bilinker adentro de lattice.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| La spec de bilinker dice que el nivel 1 es el único nivel aceptado del vecindario, y que lo derivado es de lattice | `cerrada` | `ACC-384` | `accept.md` § "Bilinker no sale a buscarlos" ↔ `Lspd` en `lspd_neighbours.rs` |

### 2. Los comandos no levantan el daemon, y le preguntan al nivel 1 con la misma regla

**`check`, `apply` y `accept` usan el daemon activo y nunca lo levantan.** Si hay que resolver un nivel 1 y no contesta, fallan antes de trabajar y nombran las dos salidas, con los lenguajes que hacen falta:

```
error: 108 endpoints tienen nivel 1 y no hay daemon en esta capa.
  Levantarlo:       lspd start --wait --lang java --lang typescript
  Sin confirmarlo:  bilinker check . --no-ask-n1
```

| Flag | Comandos | Qué hace |
|---|---|---|
| sin flag | los tres | Usa el daemon activo: si indexa, espera; si no está o falla, el comando falla. |
| `--no-ask-n1` | los tres | No le pregunta a nadie en esta corrida. Cada comando hace lo que git y tree-sitter alcanzan, y dice lo que no confirmó. |
| `--decline-n1` | `accept` | Escribe `declined`. Sobre un nivel 1 adquirido pide además `--force`. |

**Un daemon que indexa se espera, y un daemon que falla hace fallar el comando.** Vale para el que levantó cualquiera, y para cualquier pregunta: un `-32001` es *"todavía no"*, y se espera mirando `status`, diciendo qué servidor indexa; un servidor que no está instalado, que se cae o que no soporta el lenguaje es una falla. Ctrl-C durante la espera corta el comando, que tampoco sigue sin vecindario. No hay un tercer resultado entre *"contestó"* y *"falló"*: lo que antes era *"pregunté y no pude"* es un comando que no terminó.

**Levantar y apagar el daemon es un paso explícito, fuera de los comandos:** `lspd start --wait` y `lspd stop` (§ 9). El costo nunca estuvo en arrancar sino en que nadie apagara, y un comando que arranca deja el apagado sin dueño. Así se juntaron diecisiete `rust-analyzer` en una sesión.

**`--no-n1` desaparece.** Con un significado nuevo, la línea que lo tiene escrito —un CI, o el `accept --no-n1 .` de `sge`— haría otra cosa sin que nadie se entere; sin el flag, falla y obliga a elegir entre no preguntar y renunciar.

**Se elige que no preguntar sea explícito.** El default no afirma menos de lo que puede afirmar, y un CI sin language server lo declara una vez con `--no-ask-n1`, que nunca baja cobertura: bajarla es `--decline-n1`, que no se escribe sin darse cuenta.

**Se descarta un flag por comando que levante el daemon.** Repite en cada invocación lo que `lspd start --wait` hace una vez, y vuelve a dejar el apagado sin dueño. **Se descarta un `bilinker ask-n1 start` y `end`:** el ciclo de vida del daemon es de `lspd`, y lattice usa el mismo, así que un `end` de bilinker le apagaría el daemon a lattice. Lo único que aportaba, qué lenguajes calentar, lo dice el mensaje de error. **Se descarta no preguntar por defecto:** lo sin confirmar se vuelve el estado normal, y lo que sale siempre no lo lee nadie. **Y se descarta que el mismo flag diga "no preguntes" en un comando y "renuncio" en otro:** uno vale una corrida y el otro queda escrito.

**Avance: 5/5.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Los tres usan el daemon activo, no lo levantan, y fallan sin él nombrando las salidas y los lenguajes | `cerrada` | `ACC-384` | `accept.md` § "Los comandos usan el daemon activo y nunca lo levantan" ↔ `require`, `available`, `demand`; `check.md` § "`check` usa el daemon activo y nunca lo levanta" ↔ `n1_demand` |
| `--no-ask-n1` en los tres | `cerrada` | `ACC-384` | `check.rs` (`compare_contract`), `apply.rs` (`moved_neighbours`), `accept.rs` (`resolve_n1`), con sus reglas en `check.md`, `apply.md` y `accept.md` |
| `--decline-n1` reemplaza a `--no-n1` en `accept` | `cerrada` | `ACC-384` | `accept.md` § "`--decline-n1` renuncia, y `--no-ask-n1` no pregunta" ↔ `resolve_n1` |
| El adaptador sin `spawn`, y espera a cualquier daemon que indexa | `cerrada` | `ACC-384` | `accept.md` § "Espera a cualquier daemon que indexa, y Ctrl-C corta el comando" ↔ `wait_ready`, `of_with` en `lspd_neighbours.rs` |
| Una falla del daemon hace fallar el comando, y Ctrl-C lo corta | `cerrada` | `ACC-384` | la misma regla ↔ `wait_ready`, `of_with` |

### 3. `check` confirma el nivel 1, y lo que no confirma lo dice `OK_N1_UNCONFIRMED`

**Sin daemon, o con un daemon que falla, `check` sale con 2**, como con una versión que no entiende: no terminó de verificar. Lo que alcanzó a verificar queda en la cache, como en cualquier check parcial. Una capa sin ningún nivel 1 adquirido —sin `n`, o todo `declined`— no pide daemon ni flag.

**Con o sin daemon, el contenido de los vecinos se mira con tree-sitter.** Se resuelven los captures de los vecinos aceptados y se pliegan como el fold: un cambio ahí es drift probado y no necesita a nadie. El daemon agrega lo único que los captures no pueden decir, que los nombres de la firma sigan resolviendo a esos vecinos.

**`OK_N1_UNCONFIRMED` es un endpoint sano en todo lo que git y tree-sitter pueden mirar, con la resolución de los nombres sin confirmar.** Sale sólo con `--no-ask-n1`: el fragmento está `OK`, los vecinos aceptados no cambiaron, y nadie preguntó si la firma sigue nombrando a esos vecinos. Sale con 0.

| Estado | Nivel 0 | Contenido de los vecinos aceptados | Resolución de los nombres |
|---|---|---|---|
| `OK` | ok | igual | confirmada |
| `OK_N1_UNCONFIRMED` | ok | igual | no se preguntó: `--no-ask-n1` |
| `CONTRACT_RESTYLED`, `CONTRACT_ALTERED` | ok | distinto | no hace falta |

- **Aplica sólo a un nivel 1 adquirido,** también al vacío: un import nuevo puede meterle un vecino. Sin `n` o con `declined`, el estado es `OK`.
- **Lo probado le gana a lo sospechado:** `CONTRACT_ALTERED` y `CONTRACT_RESTYLED`, después `CONTRACT_RELOCATED` y `CONTRACT_UNLOCATED`, después `OK_N1_UNCONFIRMED`, y por último `OK`.
- **Un vecino cuyo capture ya no resuelve es un cambio, no una ausencia:** es `CONTRACT_ALTERED`.
- **No se lista uno por uno.** Sale en el resumen, con cuántos son y qué correr para confirmarlos, y se listan filtrando por el estado.

**`CONTRACT_UNVERIFIED` desaparece.** Sin flag, `check` espera al servidor que indexa o falla; con `--no-ask-n1`, lo no confirmado es `OK_N1_UNCONFIRMED`. Con un 0 sin flag, todo nivel 1 quedó confirmado.

**Y se descarta dar `OK` con los captures solos:** diría OK sobre una resolución que nadie miró.

**Avance: 4/5.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Sin daemon o con uno que falla, `check` sale con 2 | `sin bilink` | `ACC-384` | `check.md` § "Código de salida de `check`" está atada a esta sección, y no al código que sale con 2 en `bilinker-cli/src/main.rs` |
| `CONTRACT_UNVERIFIED` sale del estado y de la spec | `cerrada` | `ACC-384` | `check.md` § "Los estados de aceptación" ↔ `EndpointState` en `state.rs` |
| El contenido del nivel 1 se verifica con los captures, con o sin daemon | `cerrada` | `ACC-384` | `check.md` § "El contenido del nivel 1 se verifica con los captures de los vecinos" ↔ `fold_captures` |
| `OK_N1_UNCONFIRMED`, con su precedencia y en el resumen | `cerrada` | `ACC-384` | `check.md` § "`OK_N1_UNCONFIRMED`…" y § "Lo probado le gana a lo sospechado" ↔ `compare_contract` |
| Un vecino que no resuelve es `CONTRACT_ALTERED` | `cerrada` | `ACC-384` | `compare_contract`: un `fold_captures` sin resultado es `ContractAltered` |

### 4. `CONTRACT_UNVERIFIED` desaparece

**No queda un resultado entre *"el daemon contestó"* y *"el comando falló"*.** Todo comando espera a un daemon que indexa y falla si el daemon falla (§ 2), así que *"pregunté y no pude"* no llega a ser un estado. Sin flag, un `check` que sale con 0 confirmó todo nivel 1; lo que no se quiere confirmar se pide con `--no-ask-n1` y es `OK_N1_UNCONFIRMED` (§ 3).

**Se descarta conservarlo con 0**, como hoy: daba por confirmado lo que no se confirmó. **Y se descarta conservarlo con 1:** un CI rojo porque el servidor seguía indexando es un falso rojo, y esperarlo lo evita.

**Avance: 1/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Ningún comando produce `CONTRACT_UNVERIFIED` | `cerrada` | `ACC-384` | `check.md` § "Los estados de aceptación" ↔ `EndpointState`; sólo lo nombra un test que comprueba que no sale |

### 5. Confirmar la resolución con git, sin daemon — abierta

**Qué está en juego.** El nivel 1 aceptado tiene un commit. Si desde ahí no cambiaron los imports y el `package` del archivo del fragmento, ni se agregó, borró o renombró en la capa una declaración con el nombre de alguna posición de la firma, ni cambiaron los manifiestos de build, la resolución no pudo cambiar salvo por dependencias fuera de git. Un endpoint así no necesita daemon: `check` da `OK` sin él, y si ninguno lo necesita, no falla ni pide flag.

**Lo que falta saber es si vale:** medir, sobre la historia de `sge`, en cuántos pares de commits se dispararía. Si un manifiesto cambia en casi todos, no ahorra nada. Las reglas son de cada lenguaje.

**Avance: 0/1.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| Medir cuánto se dispara el filtro en `sge` | `abierta` | — | — |

### 6. Qué hacen `apply` y `accept` sin preguntar

**`apply` repunta `link` y `n.1.link`, y nunca aprueba.** Lo que resuelve git —el fragmento que se movió, un vecino que se mudó de archivo— no necesita daemon. Lo que sí: un conjunto que ganó o perdió miembros, llenar un `unknown` y subir la cobertura. Con `--no-ask-n1` arregla lo primero y dice cuántos vecindarios no miró. Sin daemon y sin flag falla con un código propio, porque su 2 ya dice *"no hay nada que arreglar"*.

**`accept` fija el contrato, y lo que escribe queda.** Adquiere el nivel 1 cada vez que la firma alcanza un tipo, como hoy. Con `--no-ask-n1` conserva lo que se puede conservar y falla donde no:

| `n` previo | Cambió la firma | `--no-ask-n1` |
|---|---|---|
| ausente | — | falla y nombra `lspd start --wait` y `--decline-n1` |
| `declined` | — | la conserva |
| adquirido | no | lo conserva |
| adquirido | sí | falla: el conjunto pudo cambiar con la firma |

**Sin flag, `accept` ya no conserva en silencio** un nivel 1 adquirido con la firma intacta, como hoy sin proveedor. Conservarlo no confirma la resolución, por lo mismo que en `check`, y ahora se pide con `--no-ask-n1`.

**No se agrega un camino que acepte sólo el nivel 0 sin decirlo:** es la invariante que `declined` vino a cerrar.

**Avance: 3/3.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `apply` con `--no-ask-n1` arregla lo de git y cuenta lo no mirado | `cerrada` | `ACC-384` | `apply.md` § "Con `--no-ask-n1` arregla lo que resuelve git, y cuenta lo que no miró" ↔ `moved_neighbours` |
| `apply` sin daemon y sin flag falla con un código propio | `cerrada` | `ACC-384` | `apply.md` § "Sin daemon, `apply` falla con 4 antes de trabajar" ↔ `demand` en `apply.rs` |
| `accept` con `--no-ask-n1`, según la tabla | `cerrada` | `ACC-384` | `accept.md` § "El `n` previo tiene tres valores…" ↔ `resolve_n1` |

### 7. `bilinker graph` queda como la exportación de aristas hacia lattice

**Recorrer es de lattice.** `graph` pierde `tree`, `flat` y `--depth`, y queda la salida JSON: una arista por cadena, entre sus tips, con `state`, `commit` y el rango de la cache, y `--recursive` para juntar las capas bajo la raíz. Es lo único que lattice no puede saber sin duplicar el formato.

**Un rango de varias partes sale con un tramo por parte, y con la declaración que nombra su capture.** La declaración es el nodo que declara el ancla: en un `spring-controller`, el método entero, con su cuerpo. Lattice contiene con los tramos, y con la declaración lo que cae adentro del método y fuera de las partes (§ 8). Reemplaza al tramo de la primera parte a la última: todos los endpoints de un controller empiezan en la misma anotación de la clase, y sus tramos se anidaban. Medido el 2026-09-16 sobre `sge`: `lattice graph` desde el bilink de `getDatosGraficoAusentismo` devolvía los 10 bilinks de `DashboardController`, y ahora devuelve sólo el suyo.

**Sin ninguna arista que emitir, `graph` sale con 1. Con algún bilink sin rango en la cache, emite lo que tiene y sale con 3,** y lattice deja al proveedor `bilink` `Degraded` con la razón que dio. Es el caso de un bilink creado y aceptado después del último `check`: antes `graph` lo omitía y salía con 0, y `lattice graph` de ese bilink decía `(sin aristas)` con 1, donde correspondía 3.

**Se descarta que lattice lea `.bilink/` con `bilink-format`:** sería la dependencia de crates de lattice hacia bilinker, y la resolución de cadenas y de la cache, repetida. **Se descarta una arista por parte:** rompe una arista por cadena. **Se descarta que `graph` resuelva los rangos que faltan, o que lattice corra `check`:** exportaría un rango que ningún `check` dejó, y una consulta de lattice no escribe.

**Avance: 6/7.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `graph` sin `tree`, `flat` ni `--depth` | `cerrada` | `ACC-385` | `graph.md` § "`json` es el único formato, y el que se usa sin `--format`" ↔ `cmd_graph` |
| Encuentra los bilinks `.yaml` | `sin bilink` | `ACC-385` | `find_graph_starts` filtra `.yaml`, atada a `graph.md` § "Un selector es un archivo, una posición, un UUID o toda la capa", que no está atada a esta sección |
| Sin bilinks, sale distinto de 0 | `cerrada` | `ACC-385` | `graph.md` § "Sin ninguna arista que emitir, sale con 1" ↔ `cmd_graph` |
| Un rango de varias partes sale con un tramo por parte | `cerrada` | `ACC-390` | `graph.md` § "Un rango de varias partes sale con un tramo por parte" ↔ `chain_tips` |
| Un tip de varias partes lleva la declaración de su capture | `cerrada` | `ACC-390` | `graph.md` § "Un tip de varias partes lleva la declaración que nombra su capture" ↔ `tip_declaration`, `query::declaration` |
| Con algún bilink sin rango en la cache, `graph` sale con 3 | `cerrada` | `ACC-388` | `graph.md` § "Con algún bilink sin rango en la cache, emite lo que tiene y sale con 3" ↔ `cmd_graph` |
| Un `graph` que sale con 3 deja al proveedor `bilink` `Degraded` | `cerrada` | `ACC-388` | lattice, `provider.md` § "Un `graph` que sale con 3 deja a `bilink` `Degraded`" ↔ `edges_with_gaps` |

### 8. Lattice gana `mentions`, y el impacto que sube por los llamadores

**`mentions` va de una posición de una firma a la declaración del tipo que nombra ahí.** Es `derived` y dirigida. Le sirve a impact al revés: cambió un DTO, qué firmas lo mencionan, qué bilinks las contienen.

**Para que un cambio llegue a un bilink por los llamadores, lattice tiene que:**
- **Expandir en cada nodo alcanzado,** no sólo en los de partida, con tope de profundidad.
- **Decir "no pude" por pregunta.** Un resultado vacío por un error es un estudio que no mide nada.
- **Contener contra la función que envuelve a un capture,** no sólo contra sus partes: el nodo de un llamador es el comienzo de su línea, y ninguna parte de un `spring-controller` lo cubre.
- **Subir de una línea cambiada a la función que la contiene,** que hoy es una heurística sobre la línea.

**Lo que ni el nivel 1 ni los llamadores ven**, y queda dicho: la herencia —en `sge`, 191 clases extienden `ResponseModelBase`, y el nivel 1 captura la subclase— y que el `api-model.d.ts` generado del front coincida con el DTO de Java del que sale.

**Avance: 0/5.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `mentions` declarado y emitido por el proveedor `lsp` | `pendiente` | — | — |
| La expansión en cada nodo alcanzado | `diverge` | — | `lattice-cli/src/main.rs`: `expand` sólo sobre `starts` |
| "No pude" por pregunta | `diverge` | — | `Registry::expand` (`.ok()`), `call_edges` (`unwrap_or_default`), `anchor_of` |
| La contención contra la función que envuelve al capture | `pendiente` | — | — |
| De una línea a la función que la contiene | `diverge` | — | `identifier_col` |

### 9. El daemon se levanta y se apaga afuera, y esto reemplaza `levantar-el-daemon` § 1 y § 2

**Se levanta con `lspd start --wait` y se apaga con `lspd stop`**, en cada copia de un repo. En una vista, los dos pasos son de `begin-work` y `end-work`, la opción que `levantar-el-daemon` § 3 ya sigue. Ningún comando de bilinker lo levanta ni lo apaga —sí espera al que indexa (§ 2)—, y el índice se reusa entre corridas mientras siga vivo.

**Reemplaza `levantar-el-daemon` § 1 y § 2**, que hacen que el adaptador lo levante una vez por corrida, espere sólo al que levantó y degrade a `CONTRACT_UNVERIFIED` si no puede. Las dos descartaron lo que ahora se elige, y con argumentos que no se sostienen:

- **"Que `check` no lo levante" se descartó porque el arranque se paga una vez por corrida.** Es cierto, pero el costo que se midió no es el arranque: es que nadie apague. Diecisiete `rust-analyzer` vivos llevaron una sesión a 20,9 GB, y en `sge` se renunció al nivel 1 en bloque antes que levantar `jdtls`.
- **"Una sesión dueña del daemon" se descartó porque tiraba el índice al cerrarse y no encajaba con comandos sueltos desde agentes.** `start` y `stop` alrededor de una sesión de trabajo no tiran el índice entre corridas, y los agentes ya lo hacen a mano en cada vista.

**Una espera se corta cuando el servidor deja de avanzar, no cuando pasa un tiempo.** Cuánto tarda un servidor en indexar depende del tamaño del repo —`rust-analyzer` tardó siete minutos sobre un workspace mediano, y `sge` tiene 3837 archivos Java—, así que un tope total no tiene un valor razonable. Lo que sí lo tiene es cuánto puede pasar sin progreso: la espera de los comandos y la de `lspd start --wait` fallan si un servidor sigue `INDEXING` sin reportar progreso durante una ventana, o si su proceso muere, y mientras avance esperan sin tope. Es un liveness de `lspd`: hoy deriva `INDEXING` y `READY` de `$/progress`, pero `status` no dice cuándo llegó el último, y `--timeout` es un tope total. Un servidor `RUNNING` no informa progreso, y tampoco se espera.

**Se descarta `lspd start --wait --timeout N` como protección de un CI:** obliga a elegir una duración que no se conoce, y un tope holgado deja colgada una corrida estancada tanto tiempo como un tope ajustado corta una que avanzaba.

**`lattice graph` sigue con su política**: levanta el daemon, no espera y reporta `Degraded`. Si se alinea con ésta es de lattice.

**Avance: 3/4.**

| Dimensión | Estado | Tarea | Evidencia |
|---|---|---|---|
| `lspd` expone por servidor cuándo llegó el último progreso, y la espera de `lspd-client` falla por estancamiento | `cerrada` | `ACC-386` | `levantar-el-daemon` § 5 en lspd, con la cadena hasta el código aceptada ahí: `since_progress_ms`, `assess`, `STALL`, `--stall` en vez de `--timeout`, y el servidor que muere sale del mapa |
| Medir cada cuánto reportan progreso `rust-analyzer` y `jdtls`, en accreta y en `sge`, para la ventana | `pendiente` | `ACC-386` | `rust-analyzer` medido: 6,8 s de silencio máximo, y la ventana por defecto quedó en 120 s. Falta `jdtls` sobre `sge`: no está instalado donde se midió |
| La spec de bilinker dice que los comandos no levantan el daemon | `cerrada` | `ACC-384` | `accept.md` § "Los comandos usan el daemon activo y nunca lo levantan" ↔ `require`, `available`, `demand` |
| `levantar-el-daemon` anota que § 1 y § 2 quedan reemplazadas | `cumple` | — | `levantar-el-daemon` 1.3, en el encabezado, en `45e4e08` de bilinker |

---

## Lo que esta decisión no decide

- **La forma del liveness en `lspd`:** el campo de `status`, la ventana por defecto y si `--timeout` sigue existiendo. Es de `lspd`; acá se fija que la espera se corta por estancamiento.
- **La forma de `begin-work` y `end-work`.** Es `levantar-el-daemon` § 3, y el idle timeout es de `lspd`.
- **Si `lattice graph` deja de levantar el daemon.** Es de lattice.
- **El nombre exacto y la forma de `mentions` en la spec de lattice.** Es de lattice.
- **Cómo se declaran las relaciones entre repos.** Es `arquitectura`, § 1 y § 4.
- **Cómo verifica impact que las dos puntas de un contrato coincidan.** Es de impact.

---

## Historial

| Versión | Fecha | Qué cambió |
|---|---|---|
| 0.1 | 2026-09-16 | Sale del spike sobre si el nivel 1 puede ser de lattice. Lo que bilinker hace con el nivel 1, medido por parte; lattice no tiene un `kind` para los tipos de una firma, y `lspd` sí los resuelve. Se recomienda que lattice adquiera y bilinker decida, que `check` y `apply` miren el nivel 1 sólo con `--n1`, y que `accept` no cambie. |
| 0.2 | 2026-09-16 | El corte pasa a ser por garantía: bilinker conserva el adaptador de `lspd`, y lattice se queda con lo derivado y n > 1. `check`, `apply` y `accept` no levantan el daemon: usan el activo y fallan sin él, y se levanta y se apaga afuera con `lspd start --wait` y `lspd stop`, lo que reemplaza `levantar-el-daemon` § 1 y § 2. `--no-ask-n1` no pregunta, y en `check` lo que no confirma es `OK_N1_UNCONFIRMED`. `--decline-n1` reemplaza a `--no-n1` como renuncia en `accept`. `bilinker graph` queda como exportación. Se agregan la medición de `sge`, la causa de la falla de `graph`, lo que falta en lattice para llegar a un bilink por los llamadores, y dos secciones abiertas. |
| 0.3 | 2026-09-16 | Se cierra el código de salida de `CONTRACT_UNVERIFIED`, que desaparece: todo comando espera a un daemon que indexa, y falla si el daemon falla. La § 4 queda cerrada con eso. La § 9 corta la espera por estancamiento, con un liveness de `lspd`, en vez de un `--timeout`. |
| 1.0 | 2026-09-16 | En desarrollo: las tablas se escriben desde `main` con lo que dejaron las ramas de bilinker y de `lspd`. Las secciones 1, 2, 4 y 6 quedan cerradas; la 3 y la 7 tienen una dimensión sin bilink cada una; la 9 espera la medición de `jdtls`. La parte de `lspd` es `levantar-el-daemon` § 5. |
| 1.1 | 2026-09-16 | La § 7 con lo que dejaron las ramas de bilinker y de lattice: un rango de varias partes sale con un tramo por parte y con la declaración de su capture, en vez del tramo de la primera a la última, y `graph` sale con 3 con algún bilink sin rango, que deja al proveedor `bilink` `Degraded`. Su tabla pasa a 6/7. La parte de lattice existe, con las secciones 7 y 8. |
